---
name: vibe-tools
description: Use the vibe-tools CLI for browser automation, web research, repo context, and MCP helpers. Use when the user mentions vibe-tools, cursor-tools, or needs agent-side web/repo tooling beyond built-in browser skills.
---

# vibe-tools

```bash
export PATH="$HOME/.local/bin:$PATH"
which vibe-tools || npx -y vibe-tools --help
```

Prefer command help over guessing flags. Ask the user to configure API keys/env if auth fails.
